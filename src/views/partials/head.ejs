<!doctype html>
<html lang="zh-Hant-TW">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="robots" content="noindex">
  <title><%= title %><%= locals.admin ? '｜防偽標籤後台' : '｜防偽驗證' %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body class="<%= locals.admin ? 'admin' : 'public' %>">
<% if (locals.admin) { %>
<nav class="nav">
  <strong>防偽標籤後台</strong>
  <a href="/admin/products" class="<%= nav === 'products' ? 'on' : '' %>">產品</a>
  <a href="/admin/batches" class="<%= nav === 'batches' ? 'on' : '' %>">批次</a>
  <a href="/admin/labels" class="<%= nav === 'labels' ? 'on' : '' %>">標籤查詢</a>
  <a href="/admin/suspicious" class="<%= nav === 'suspicious' ? 'on' : '' %>">可疑標籤</a>
</nav>
<% } %>
<main class="main">
